# ----------------------------------------
# Canonical Domain (HTTP only - SSL handled by Certbot)
# ----------------------------------------

# Root domain → redirect to www
server {
    listen 80;
    server_name {{ROOT_NAME}};

    return 301 http://{{SERVER_NAME}}$request_uri;
}

# Main www domain
server {
    listen 80;
    server_name {{SERVER_NAME}};

    root {{STATIC_ROOT}};
    index index.html;

    location / {
        try_files $uri /index.html;
    }
}